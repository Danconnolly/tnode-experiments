# Teranode Service Endpoints Configuration
# Copy this file to .env and customize for your local setup

# Teranode blockchain service endpoint (IP:port or just IP, default port is 8087)
#
# IMPORTANT: A full Teranode system consists of multiple services. This endpoint
# should point to the blockchain service component specifically, which handles
# blockchain operations like block headers, chain state, etc.
#
# Examples:
#   BLOCKCHAIN_ENDPOINT=127.0.0.1        # Will use 127.0.0.1:8087
#   BLOCKCHAIN_ENDPOINT=127.0.0.1:8087   # Explicit port
#   BLOCKCHAIN_ENDPOINT=192.168.1.100    # Will use 192.168.1.100:8087
#   BLOCKCHAIN_ENDPOINT=node.example.com:9000  # Custom domain and port
BLOCKCHAIN_ENDPOINT=127.0.0.1:8087

# Teranode peer service endpoint (IP:port or just IP, default port is 8088)
#
# This endpoint should point to the peer service component, which handles
# peer-to-peer network operations, peer management, and network topology.
#
# Examples:
#   PEER_ENDPOINT=127.0.0.1           # Will use 127.0.0.1:8088
#   PEER_ENDPOINT=127.0.0.1:8088      # Explicit port
#   PEER_ENDPOINT=192.168.1.100:9001  # Custom IP and port
PEER_ENDPOINT=127.0.0.1:8088
